# External PostgreSQL Database Configuration (MANDATORY)
# This file contains the external database connection details

# External Telethon Database Connection
DB_HOST=18.234.56.13
DB_PORT=5432
DB_NAME=telethon_db
DB_USER=arcx
DB_PASSWORD=Ravan121

# Database Connection Settings - OPTIMIZED FOR SPEED
DB_POOL_MIN_SIZE=10          # Increased for faster connections
DB_POOL_MAX_SIZE=50          # Higher pool for concurrency
DB_TIMEOUT=60                # Longer timeout for stability

# PERFORMANCE OPTIMIZATION SETTINGS
MAX_ACTIVE_CLIENTS=100       # Maximum concurrent Telegram clients
BATCH_SIZE=25                # Larger batches for efficiency
MAX_ACCOUNTS_PER_OPERATION=75 # More accounts per operation

# API RATE LIMITING - OPTIMIZED
CALLS_PER_MINUTE_PER_ACCOUNT=30  # Increased from 20
CALLS_PER_HOUR_PER_ACCOUNT=750   # Increased from 500

# REQUEST DELAYS - REDUCED FOR SPEED
DEFAULT_DELAY_MIN=0.5        # Reduced from 1 second
DEFAULT_DELAY_MAX=2          # Reduced from 5 seconds
VIEW_BOOST_DELAY_MIN=1       # Reduced from 2 seconds  
VIEW_BOOST_DELAY_MAX=4       # Reduced from 8 seconds
REACTION_DELAY_MIN=1         # Reduced from 3 seconds
REACTION_DELAY_MAX=5         # Reduced from 10 seconds

# CACHE SETTINGS
ENABLE_CACHING=true
CACHE_TTL_SECONDS=300
CACHE_CLEANUP_INTERVAL=600

# HTTP CLIENT OPTIMIZATIONS
HTTP_MAX_CONNECTIONS=200
HTTP_KEEPALIVE_CONNECTIONS=100
HTTP_KEEPALIVE_EXPIRY=300
ENABLE_HTTP2=true

# BATCHING OPTIMIZATIONS  
ENABLE_REQUEST_BATCHING=true
BATCH_FLUSH_INTERVAL=1.5
PRIORITY_BATCH_SIZE=10

# CIRCUIT BREAKER SETTINGS
CB_FAILURE_THRESHOLD=3
CB_RECOVERY_TIMEOUT=30
